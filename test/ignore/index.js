'use strict'

/**
 * Dependencies
 */
var should = require('chai').should(),
    expect = require('chai').expect;

describe('# Test', function() {
  it('look mama, I\'m a test', function() {
    expect(true).to.equal(true);
  });
});
